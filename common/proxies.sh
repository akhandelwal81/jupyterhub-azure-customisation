##This could be used to setup proxies if your implementation  is within enterprise network and way to access external website is via Proxy servers
export no_proxy=
export http_proxy=
export ftp_proxy=
export https_proxy=
